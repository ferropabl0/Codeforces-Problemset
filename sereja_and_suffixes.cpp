#include <bits/stdc++.h>

using namespace std;

int main()
{
    freopen("input.txt", "r", stdin);
    int n, m, auxi, sum = 0;
    cin >> n >> m;
    vector<int> els, aux;
    while (n--)
    {
        cin >> auxi;
        els.push_back(auxi);
    }
    aux = els;
    for (int i = 0; i < n; i++)
    {
        }

    while (m--)
    {
        cin >> auxi;
    }

    return 0;
}